// Reduced Specification Standard Library `types.fx` -> `redtypes.fx`

namespace standard
{
	namespace types
	{
        unsigned data{8} as byte;

        byte[] as noopstr;

        char nl = "\n";
	};
};