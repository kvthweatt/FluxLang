// Does not compile yet
def main() -> int
{
	heap x = 5;  // Must implement in parser + AST
    stack y = 3; // "

    (void)x;     // Must use a disassembler to inspect memory, ensure this is freed
    (void)y;     // "

	return 0;
};